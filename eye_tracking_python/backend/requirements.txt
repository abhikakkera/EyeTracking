# PDEYE backend dependencies (in addition to the tracker's requirements.txt).
# Install with:  pip install -r backend/requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0          # used by FastAPI TestClient in the test suite
