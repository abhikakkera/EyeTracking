# PDEYE ML pipeline — dependencies
# Deliberately does NOT require opencv/mediapipe: this layer only reads the
# tracker's exported CSV/JSON, so it installs cleanly on any Python (incl. 3.13).
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
# Optional: only needed if you add ROC/feature plots.
matplotlib>=3.7
